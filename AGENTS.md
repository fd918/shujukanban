# 看板项目专属协作规则

## 双版本定义

- **本地版本**：当前 Mac 本机部署的版本，以及由本机代码推送到 GitHub 后运行的版本。两者按同一个“本地版本”管理。
- **服务器版本**：部署在远程服务器上，通过“服务器 IP + 端口”访问的版本。

## 默认同步与发布规则

1. 后续所有功能新增、修改和修复，默认同时应用到本地版本和服务器版本，保证两个版本的功能与行为一致。
2. 每次修改应先完成必要验证，再同步本地运行版本、GitHub 运行版本和服务器版本，并分别检查可访问性及核心功能。
3. 服务器版本默认由 Codex 直接更新，不需要用户每次重复授权；执行前仍需说明影响范围、涉及文件、服务是否需要重启以及回退方式。
4. 本地版本同步所必需的 Git 提交和推送，视为本项目双版本发布流程的一部分；提交前应先检查改动范围，避免带入无关文件或敏感信息。
5. 只有用户明确说明“本次只修改服务器版本”时，才不把该次改动同步到本地版本和 GitHub 运行版本。
6. 如果服务器地址、部署目录、连接方式、启动或重启命令等信息无法从项目现有配置中确认，不得猜测；应先完成不依赖这些信息的本地修改与验证，再向用户说明缺失信息。
7. 同一项目多个对话涉及服务器更新时，必须严格串行发布：当前发布完成备份、替换、重启和健康检查后，下一个对话才能开始发布；不得并行覆盖正式目录。

## 安全要求

- 两个版本都不得把账号、密码、Cookie、Token、Webhook、私钥或其他敏感信息写入代码、公开文件或 Git 提交。
- 服务器更新前保留可回退的版本记录；涉及配置、数据或服务重启时，按全局规则先确认影响并验证回退路径。
